[project]
name = "pet2bids-prototype"
version = "0.1.0"
description = "Reference oracle for BIDSvue M12 PET importer. Wraps pypet2bids end-to-end against the PET2BIDS example data and confirms bids-validator passes."
requires-python = ">=3.11"
dependencies = [
    "pypet2bids==1.4.6",
]

[tool.uv]
package = false
